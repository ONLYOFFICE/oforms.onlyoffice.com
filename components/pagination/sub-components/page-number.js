import React from "react";
import StyledPageNumber from "./styled-page-number";

const PageNumber = ({...rest}) => {
    return (
        <StyledPageNumber {...rest}>
        </StyledPageNumber>
    );
};

export default PageNumber;